
version: '2'

services:
  web:
    container_name: web
    image: nginx:latest

    ports:
     - "80:80"
    volumes:
    - ./Main-Implemented:/code
    - ./docker/nginx/site.conf:/etc/nginx/conf.d/site.conf

  phpfpm:
    container_name: phpfpm
    image: php:7.2-fpm
    volumes:
    - ./Main-Implemented:/code

